package normal

import "yath/linkedList"

type LRUCache struct {
	MyLinkdList  *linkedList.DNode
	MyAddressMap map[linkedList.DNode]linkedList.DNode
}

func (lru *LRUCache) AddNodeToLRUCache(inp ...int) {
	for 
	data := linkedList.GetDNode(inp)
	lru.MyLinkdList
}
